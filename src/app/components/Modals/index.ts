import {
  registerModal,
  registerModalWithId,
  showModal,
  showModalById,
  showModalByType,
} from "./modalHandler";

export { registerModal, showModal, showModalByType, registerModalWithId, showModalById };
